<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bgelfius.c196_wgu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <provider
            android:name=".DBProvider"
            android:authorities="com.example.bgelfius.c196_wgu.dbprovider"
            android:exported="false" />


        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ListActivity"
            android:label="Detail Screen"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".TermDetailActivity"
            android:label="Detail Screen"
            android:theme="@style/AppTheme" />
        <activity
        android:name=".CourseDetailActivity"
        android:label="Detail Screen"
        android:theme="@style/AppTheme" />
        <activity
            android:name=".AssessmentDetailActivity"
            android:label="Detail Screen"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".EditChangeTerm"
            android:label="Detail Screen"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".EditChangeCourse"
            android:label="Detail Screen"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".EditChangeCourseNote"
            android:label="Detail Screen"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".EditChangeMentor"
            android:label="Detail Screen"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".EditChangeAssessment"
            android:label="Detail Screen"
            android:theme="@style/AppTheme" />
</application>
</manifest>